{
  "happyscribe": {
    "description": "Happy Scribe hosted automatic speech-to-text via signed upload and orders",
    "type": "api",
    "models": {
      "auto": {
        "description": "Automatic Happy Scribe STT with fixed en-US language and diarization enabled by default",
        "pricingSourceUrl": "https://www.happyscribe.com/api/pricing",
        "pricingCheckedAt": "2026-05-31",
        "pricingCurrency": "USD",
        "pricingTier": "Happy Scribe Product API automatic transcription reference pricing",
        "pricingNotes": "Happy Scribe automatic transcription is estimated at $0.60/hour. Subscription differences, enterprise pricing, taxes, and provider credits are ignored.",
        "costPerHourUSD": 0.6,
        "costPerHourCents": 60,
        "limits": {
          "notes": "Happy Scribe documents a signed-upload plus orders workflow for automatic transcription, but current public Product API docs do not publish fixed upload-size or audio-duration caps for automatic STT. AutoShow keeps Happy Scribe v1 split policy note-only and relies on explicit --split or retry-with-split if the provider rejects an oversized input."
        },
        "estimation": {
          "costMultiplier": 1,
          "msPerSecond": 240
        }
      }
    }
  }
}
