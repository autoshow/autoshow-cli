# Amazon S3 Setup and Configuration

## Prerequisites

Before using the `--save s3` option, ensure you have:

1. **AWS CLI installed and configured** with appropriate credentials:
   ```bash
   aws configure
   ```

2. **Required AWS permissions** for S3 operations:
   - `s3:CreateBucket`
   - `s3:PutObject`
   - `s3:PutBucketVersioning`
   - `s3:PutBucketLifecycleConfiguration`
   - `s3:PutBucketPublicAccessBlock`
   - `s3:HeadBucket`
   - `sts:GetCallerIdentity`

## Configuration

### Setting Up AWS Credentials

1. **Install AWS CLI**:
   ```bash
   # macOS
   brew install awscli

   # Linux
   curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
   unzip awscliv2.zip
   sudo ./aws/install
   ```

2. **Configure AWS credentials**:
   ```bash
   aws configure
   ```
   
   When prompted, enter:
   - AWS Access Key ID: Your AWS access key
   - AWS Secret Access Key: Your AWS secret key
   - Default region name: `us-east-1` (or your preferred region)
   - Default output format: `json`

3. **Set environment variables** (optional):
   ```bash
   export AWS_REGION=us-west-2  # Optional, defaults to us-east-1
   ```

### IAM Policy Example

Create an IAM policy with the minimum required permissions:

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": [
        "s3:CreateBucket",
        "s3:HeadBucket",
        "s3:ListBucket",
        "s3:PutObject",
        "s3:PutBucketVersioning",
        "s3:PutBucketLifecycleConfiguration",
        "s3:PutBucketPublicAccessBlock"
      ],
      "Resource": [
        "arn:aws:s3:::autoshow-*",
        "arn:aws:s3:::autoshow-*/*"
      ]
    },
    {
      "Effect": "Allow",
      "Action": [
        "sts:GetCallerIdentity"
      ],
      "Resource": "*"
    }
  ]
}
```

## Usage Examples

### Basic S3 Upload

```bash
# Process a single video and upload to S3
npm run as -- text --video "https://www.youtube.com/watch?v=MORMZXEaONk" \
  --whisper large-v3-turbo \
  --chatgpt gpt-4o-mini \
  --save s3
```

### Custom Bucket Prefix

```bash
# Use a custom bucket prefix instead of the default "autoshow"
npm run as -- text --video "https://www.youtube.com/watch?v=MORMZXEaONk" \
  --save s3 \
  --s3-bucket-prefix "my-podcast"
```

### Region-Specific Deployment

```bash
# Deploy to a specific AWS region
export AWS_REGION=eu-west-1
npm run as -- text --rss "https://feeds.transistor.fm/fsjam-podcast" \
  --last 5 \
  --save s3
```

## Integration Examples

### Access from AWS CLI

```bash
# List uploaded files
aws s3 ls s3://autoshow-123456789012-us-west-2/

# Download a specific file
aws s3 cp s3://autoshow-123456789012-us-west-2/1755334409936/video.md ./local-file.md

# Sync entire session folder
aws s3 sync s3://autoshow-123456789012-us-west-2/1755334409936/ ./local-folder/
```

### Generate Pre-signed URLs

For temporary sharing without credentials:

```bash
# Generate a pre-signed URL valid for 1 hour
aws s3 presign s3://autoshow-123456789012-us-west-2/1755334409936/video.md \
  --expires-in 3600
```

### Using AWS SDK in Applications

```javascript
// Example: Accessing uploaded files from Node.js
import { S3Client, GetObjectCommand } from "@aws-sdk/client-s3";

const client = new S3Client({ region: "us-west-2" });
const command = new GetObjectCommand({
  Bucket: "autoshow-123456789012-us-west-2",
  Key: "1755334409936/video-chatgpt-shownotes.md"
});

const response = await client.send(command);
const content = await response.Body.transformToString();
```

## Troubleshooting

### Permission Errors

If you encounter permission errors, ensure your credentials have the required permissions:

```bash
# Test your credentials
aws sts get-caller-identity

# Test S3 access
aws s3 ls
```

### Region Issues

If bucket creation fails, ensure you have the correct region configured:

```bash
# Set region explicitly
export AWS_REGION=us-west-2

# Or include in command
AWS_REGION=us-west-2 npm run as -- text --video "URL" --save s3
```

### Bucket Already Exists

If you get a "bucket already exists" error with a custom prefix, try a different prefix:

```bash
npm run as -- text --video "URL" --save s3 --s3-bucket-prefix "unique-prefix-2025"
```

## Cost Optimization

### Lifecycle Policies

The default lifecycle policy deletes files after 90 days. To modify this:

```bash
# Create a custom lifecycle policy JSON file
cat > lifecycle.json << EOF
{
  "Rules": [{
    "ID": "DeleteOldFiles",
    "Status": "Enabled",
    "Expiration": {
      "Days": 30
    }
  }]
}
EOF

# Apply to your bucket
aws s3api put-bucket-lifecycle-configuration \
  --bucket autoshow-123456789012-us-west-2 \
  --lifecycle-configuration file://lifecycle.json
```

### Storage Classes

To use cheaper storage classes for infrequent access:

```json
# Transition to Infrequent Access after 30 days
{
  "Rules": [{
    "ID": "TransitionToIA",
    "Status": "Enabled",
    "Transitions": [{
      "Days": 30,
      "StorageClass": "STANDARD_IA"
    }],
    "Expiration": {
      "Days": 90
    }
  }]
}
```

## Security Best Practices

1. **Use IAM roles** instead of access keys when running on EC2
2. **Enable MFA** for production AWS accounts
3. **Rotate access keys** regularly
4. **Use bucket policies** to restrict access to specific IP ranges
5. **Enable CloudTrail** for audit logging
6. **Enable S3 Object Lock** for compliance requirements
7. **Use S3 Encryption** for sensitive content

### Example Bucket Policy

```json
{
  "Version": "2012-10-17",
  "Statement": [{
    "Sid": "RestrictToCompanyIP",
    "Effect": "Allow",
    "Principal": {
      "AWS": "arn:aws:iam::123456789012:user/autoshow-user"
    },
    "Action": "s3:*",
    "Resource": [
      "arn:aws:s3:::autoshow-123456789012-us-west-2",
      "arn:aws:s3:::autoshow-123456789012-us-west-2/*"
    ],
    "Condition": {
      "IpAddress": {
        "aws:SourceIp": ["203.0.113.0/24"]
      }
    }
  }]
}
```